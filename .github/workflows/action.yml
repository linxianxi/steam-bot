name: Puppeteer Steam Scraper

# 通过 API 手动触发（外部 cron 等调度器）
on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    environment: production

    steps:
      # 检出仓库代码
      - name: Checkout code
        uses: actions/checkout@v5

      # 安装 Node.js 20 并启用 Yarn 缓存
      - name: Setup Node.js 20 with Yarn cache
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"
          # 如果你的 yarn.lock 不在根目录，需要指定 cache-dependency-path
          cache-dependency-path: "yarn.lock"

      # 安装依赖
      - name: Install dependencies
        run: yarn install --immutable

      - name: Run Puppeteer script
        run: yarn start
        env:
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
          PUSH_PHONE_URL: ${{ secrets.PUSH_PHONE_URL }}
          PHONE: ${{ secrets.PHONE }}
          AI_TOKEN: ${{ secrets.AI_TOKEN }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 运行完成
      - name: Done
        run: echo "🚀 Puppeteer scraping finished"
